# Prevent renv from writing repository info to renv.lock
Sys.setenv(RENV_CONFIG_SNAPSHOT_REPOS = "FALSE")

# Prevent renv from overriding repos set in Rprofile.site
Sys.setenv(RENV_CONFIG_REPOS_OVERRIDE = "")

# Activate renv
source("renv/activate.R")
